version: 2.1

description: >
  Use this orb to get secrets from AWS secrets manager more efficiently

# If your orb requires other orbs, you can import them like this. Otherwise remove the "orbs" stanza.
# orbs:
#  hello: circleci/hello-build@0.0.5
